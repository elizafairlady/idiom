#!/usr/bin/env ish
true && echo and-ran
false || echo or-ran
false && echo skipped
echo after-skip
r = false || echo recovered
println r
c = true && false
println c
sh -c "exit 3" | cat && echo unreached
echo done
